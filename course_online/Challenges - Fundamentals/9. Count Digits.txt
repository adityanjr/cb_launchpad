#include<iostream>
using namespace std;
int main(){
    int count = 0; 
    long long int n;
    short int d;
    cin>>n>>d;
    while (n > 0) { 
        if (n % 10 == d) 
            count++; 
        n = n / 10; 
    }
    cout<<count<<endl;    
    return 0; 
}