#include <iostream>
#include <algorithm>
using namespace std;
int knapsack(int *weight, int *price, int totalwt, int n) {
    for (int i = 0; i < n; i++) {
        int result = price[i] / weight[i];
        
    }
}
int max(int *weight, int *price, int n) {
    cmax = 0;
    for (int i; i<n, i++) {
    }
}
int main() {
    int weight[100];
    int price[100];
    int n = sizeof(weight) / sizeof(int);
    int 
    return 0;
}